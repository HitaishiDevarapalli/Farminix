import React from 'react';

// CheckoutModal is neutralized per user requirement (Checkout is now a dedicated full-screen page at /checkout).
export const CheckoutModal: React.FC = () => {
  return null;
};
